#include <stdio.h>
#include<string.h>
void main()
{
int n,sum=0,i;
printf("enter the size ");
scanf("%d",&n);
int *p;
p=(int*)malloc(n*sizeof(int));
printf("enter the elements ");
for(i=0;i<n;i++)
scanf("%d",p+i);
for(i=0;i<n;i++)
sum=sum+ *(p+i);
printf("sum=%d",sum);
}
 
    
